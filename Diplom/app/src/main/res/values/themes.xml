<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Основная тема приложения -->
    <style name="Theme.SmartGuard" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <item name="colorPrimary">#6200EE</item>
        <item name="colorPrimaryDark">#3700B3</item>
        <item name="colorAccent">#03DAC5</item>
        <item name="android:statusBarColor">#3700B3</item>
    </style>

    <!-- 🔥 Тема для окна предупреждения (ПРОЗРАЧНАЯ) -->
    <!-- Родитель должен быть Theme.MaterialComponents, иначе MaterialCardView не сработает -->
    <style name="Theme.SmartGuard.Translucent" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Делаем окно прозрачным -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>

        <!-- Обязательные атрибуты для MaterialComponents -->
        <item name="colorPrimary">#FF0000</item>
        <item name="colorPrimaryDark">#CC0000</item>
        <item name="colorAccent">#FF0000</item>
        <item name="colorSurface">#FFFFFF</item>

        <!-- Стиль для карточек внутри Activity -->
        <item name="materialCardViewStyle">@style/Widget.MaterialComponents.CardView</item>
    </style>

</resources>